# Operator Runbook

- Use `Omega Third Eye` when the code path is still unknown.
- Use `God Plan Mode` after discovery when execution sequencing is the blocker.
- Use `God Plan Critic` before execution when the plan needs stress testing.
- Use `Omega Conductor` only after the approved plan exists.
- Use `Omega Proof Lock` at closeout when evidence must be named honestly.
