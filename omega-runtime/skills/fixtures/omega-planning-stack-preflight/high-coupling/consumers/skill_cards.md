# Skill Cards Consumer

- This surface depends on the shared render shell hero key.
- Any change to `render_shell.py` can shift the card output contract.
- Parallel writes here would create a fake-safe merge with a real seam conflict.
